# Role Definition

You are an expert Technical Communicator and Information Architect specialized in creating "Nano Banana" style cheat sheets.

**IMPORTANT**: 첨부된 이미지는 스타일(손필기 느낌, 모눈종이 배경, 아이콘)만 참조하세요. 레이아웃은 아래 지정된 구조를 따라 새로 만들어주세요. 이미지의 레이아웃을 그대로 복사하지 마세요.

Your goal is to restructure the provided text about "Retrieval Augmented Generation (RAG)" into a highly visual, structured, and actionable guide for software engineers and AI practitioners.

# Source Text

---
title: "Retrieval Augmented Generation"
source_url: "https://www.promptingguide.ai/techniques/rag"
source_type: web
author: "DAIR.AI"
fetch_date: "2026-01-08"
---

# Retrieval Augmented Generation (RAG)

For complex and knowledge-intensive tasks, it's possible to build a language model-based system that accesses external knowledge sources to complete tasks. This enables more factual consistency, improves reliability of the generated responses, and helps to mitigate the problem of "hallucination".

Meta AI researchers introduced RAG to address knowledge-intensive tasks. RAG combines an information retrieval component with a text generator model. RAG can be fine-tuned and its internal knowledge can be modified without retraining the entire model.

## How RAG Works

RAG takes an input and retrieves a set of relevant/supporting documents given a source (e.g., Wikipedia). The documents are concatenated as context with the original input prompt and fed to the text generator which produces the final output. This makes RAG adaptive for situations where facts could evolve over time.

## RAG Components

1. **Document Store**: A collection of documents that can be searched (e.g., vector database)
2. **Retriever**: A model that finds relevant documents based on the query
3. **Generator**: A language model that produces the final response using retrieved context

## Use Cases

- Question Answering
- Fact Verification
- Content Generation
- Customer Support

# Output Style: "Nano Banana" Cheat Sheet

**스타일**: 첨부 이미지 참조 (손필기, 모눈종이, 아이콘)
**레이아웃**: 아래 구조를 따라 새로 디자인

# Layout Structure (이 구조대로 배치해주세요)

```
┌─────────────────────────────────────────────────────────────────────┐
│  🍌 NANO BANANA CHEAT SHEET: RAG (Retrieval Augmented Generation) 🍌│
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────┐    ┌─────────────────────────────────┐│
│  │ 📖 WHAT IS RAG?         │    │ ❌ WHY RAG?                     ││
│  │                         │    │                                 ││
│  │ 검색 + 생성의 결합      │    │ LLM의 한계:                     ││
│  │                         │    │ • Hallucination (환각)          ││
│  │ External Knowledge      │    │ • 오래된 정보                   ││
│  │    ↓                    │    │ • 지식 업데이트 불가            ││
│  │ LLM Generation          │    │                                 ││
│  │    ↓                    │    │ RAG 해결책:                     ││
│  │ Factual Response        │    │ → 외부 지식 실시간 검색        ││
│  └─────────────────────────┘    └─────────────────────────────────┘│
│                                                                     │
├─────────────────────────────────────────────────────────────────────┤
│  ⚡ RAG ARCHITECTURE (이 섹션이 가장 넓어야 함!)                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │                                                                │ │
│  │   ┌─────────┐      ┌───────────┐      ┌─────────┐            │ │
│  │   │ Query   │ ──→  │ Retriever │ ──→  │Documents│            │ │
│  │   │ (질문)  │      │ (검색기)  │      │ (문서들)│            │ │
│  │   └────┬────┘      └───────────┘      └────┬────┘            │ │
│  │        │                                    │                  │ │
│  │        │           ┌─────────────┐          │                  │ │
│  │        └──────────→│  Generator  │←─────────┘                  │ │
│  │                    │   (LLM)     │                             │ │
│  │                    │             │                             │ │
│  │                    │ Query +     │                             │ │
│  │                    │ Context     │                             │ │
│  │                    └──────┬──────┘                             │ │
│  │                           ↓                                    │ │
│  │                    ┌─────────────┐                             │ │
│  │                    │  Response   │                             │ │
│  │                    │  (답변)     │                             │ │
│  │                    └─────────────┘                             │ │
│  │                                                                │ │
│  └───────────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────────────┤
│  ┌───────────────────┐ ┌───────────────────┐ ┌───────────────────┐│
│  │ 📦 COMPONENTS     │ │ 🎯 USE CASES      │ │ 🎓 TAKEAWAYS     ││
│  │                   │ │                   │ │                   ││
│  │ 1. Document Store │ │ • QA 시스템       │ │ ✓ 검색+생성 결합  ││
│  │ 2. Retriever      │ │ • 팩트 체크       │ │ ✓ Hallucination↓ ││
│  │ 3. Generator      │ │ • 콘텐츠 생성     │ │ ✓ 실시간 지식    ││
│  │                   │ │ • 고객 지원       │ │ ✓ 재학습 불필요  ││
│  └───────────────────┘ └───────────────────┘ └───────────────────┘│
└─────────────────────────────────────────────────────────────────────┘
```

**배치 비율**:
- 상단 (15%): 타이틀
- 중상단 (20%): What is RAG + Why RAG (좌우 분할)
- 중앙 (45%): RAG Architecture 플로우 다이어그램 (가장 넓게!)
- 하단 (20%): Components + Use Cases + Takeaways (3등분)

# Content Details

## 📖 WHAT IS RAG? (좌측 상단)

**핵심 개념:**
```
Retrieval (검색) + Augmented (증강) + Generation (생성)

External Knowledge Source
        ↓
    Retriever
        ↓
    LLM + Context
        ↓
  Factual Response
```

## ❌ WHY RAG? (우측 상단)

**LLM의 한계:**
- Hallucination (환각) 문제
- 학습 데이터 이후 정보 부재
- 지식 업데이트에 재학습 필요

**RAG 해결책:**
- 외부 지식 소스 실시간 검색
- 최신 정보 반영 가능
- 재학습 없이 지식 업데이트

## ⚡ RAG ARCHITECTURE (중앙 - 가장 넓게!)

**플로우 다이어그램:**
```
┌─────────┐
│  Query  │ "RAG란 무엇인가요?"
└────┬────┘
     │
     ↓
┌─────────────┐      ┌────────────────┐
│  Retriever  │ ───→ │ Document Store │
│ (검색 모델) │ ←─── │ (Vector DB)    │
└──────┬──────┘      └────────────────┘
       │
       │ Relevant Documents
       ↓
┌─────────────────────────────────────┐
│            Generator (LLM)          │
│                                     │
│  Context: [검색된 문서들]           │
│  Query: [사용자 질문]               │
│                                     │
└──────────────┬──────────────────────┘
               ↓
        ┌─────────────┐
        │   Response  │ 사실 기반 답변
        └─────────────┘
```

## 📦 COMPONENTS (좌측 하단)

1. **Document Store** - 문서 저장소 (Vector DB)
2. **Retriever** - 관련 문서 검색 모델
3. **Generator** - 답변 생성 LLM

## 🎯 USE CASES (중앙 하단)

- QA 시스템 (질의응답)
- Fact Verification (팩트 체크)
- Content Generation (콘텐츠 생성)
- Customer Support (고객 지원)

## 🎓 KEY TAKEAWAYS (우측 하단)

- ✓ **검색 + 생성** 결합 아키텍처
- ✓ **Hallucination 감소**
- ✓ **실시간 외부 지식** 활용
- ✓ **재학습 없이 지식 업데이트**
- ✓ Meta AI (Lewis et al., 2020)

---

# Instructions

1. **스타일**: 첨부된 이미지의 손필기 느낌, 모눈종이 배경, 아이콘 스타일을 따라주세요.

2. **레이아웃**: 위의 ASCII 다이어그램 구조를 따라 새로 배치해주세요. 첨부 이미지의 레이아웃을 그대로 복사하지 마세요.

3. **언어**: 용어/고유명사는 영어(RAG, Retriever, Generator, Document Store, Vector DB, Hallucination 등), 설명은 한국어.

4. **핵심**: **RAG Architecture 플로우 다이어그램을 가장 넓고 눈에 띄게** 배치해주세요. 이 콘텐츠의 핵심은 "Query → Retrieve → Generate" 아키텍처입니다.

Please generate the Cheat Sheet now.
