# Role Definition

You are an expert Technical Communicator and Information Architect specialized in creating "Nano Banana" style cheat sheets.

**IMPORTANT**: 첨부된 이미지는 스타일(손필기 느낌, 모눈종이 배경, 아이콘)만 참조하세요. 레이아웃은 아래 지정된 구조를 따라 새로 만들어주세요. 이미지의 레이아웃을 그대로 복사하지 마세요.

Your goal is to restructure the provided text about "Program-Aided Language Models (PAL)" into a highly visual, structured, and actionable guide for software engineers and AI practitioners.

# Source Text

---
title: "Program-Aided Language Models"
source_url: "https://www.promptingguide.ai/techniques/pal"
source_type: web
author: "DAIR.AI"
fetch_date: "2026-01-08"
---

# Program-Aided Language Models (PAL)

PAL presents a method that uses LLMs to read natural language problems and generate programs as the intermediate reasoning steps. It differs from chain-of-thought prompting in that instead of using free-form text to obtain solution it offloads the solution step to a programmatic runtime such as a Python interpreter.

## How PAL Works

PAL represents an approach where language models generate executable programs as intermediate reasoning steps rather than relying on free-form text explanations. The model reads problems in natural language and produces programs that a runtime environment can execute to derive answers.

## Workflow

1. **Setup**: Configure API keys and instantiate the language model
2. **Prompt Engineering**: Provide few-shot examples showing how to structure problems as Python code
3. **Model Output**: The LLM generates Python code snippets
4. **Execution**: Run the generated code using Python's `exec()` function to obtain final answers

## Why PAL Works

1. **Offloads computation**: Mathematical and logical operations are handled by a reliable runtime
2. **Reduces hallucination**: The model doesn't need to "imagine" computational results
3. **Provides verifiability**: Generated code can be inspected and debugged
4. **Leverages model strengths**: LLMs are good at understanding problems and generating structured code

# Output Style: "Nano Banana" Cheat Sheet

**스타일**: 첨부 이미지 참조 (손필기, 모눈종이, 아이콘)
**레이아웃**: 아래 구조를 따라 새로 디자인

# Layout Structure (이 구조대로 배치해주세요)

```
┌─────────────────────────────────────────────────────────────────────┐
│  🍌 NANO BANANA CHEAT SHEET: PAL (Program-Aided Language) 🍌        │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────┐    ┌─────────────────────────────────┐│
│  │ 📖 WHAT IS PAL?         │    │ 🆚 CoT vs PAL                   ││
│  │                         │    │                                 ││
│  │ 자연어 → 코드 → 실행    │    │ CoT:                            ││
│  │                         │    │ Q → 자연어 추론 → A             ││
│  │ "프로그램을 중간        │    │      (계산 오류 가능)           ││
│  │  추론 단계로 사용"      │    │                                 ││
│  │                         │    │ PAL:                            ││
│  │ Gao et al. (2022)       │    │ Q → Python 코드 → exec() → A   ││
│  │                         │    │      (정확한 계산)              ││
│  └─────────────────────────┘    └─────────────────────────────────┘│
│                                                                     │
├─────────────────────────────────────────────────────────────────────┤
│  ⚡ PAL WORKFLOW (이 섹션이 가장 넓어야 함!)                         │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │                                                                │ │
│  │  ┌───────────────────────────────────────────────────────────┐│ │
│  │  │ 📝 INPUT (자연어 문제)                                     ││ │
│  │  │ "오늘은 2023년 2월 27일. 정확히 25년 전에 태어났다면       ││ │
│  │  │  생년월일은 MM/DD/YYYY 형식으로?"                          ││ │
│  │  └───────────────────────────────────────────────────────────┘│ │
│  │                           ↓                                    │ │
│  │  ┌───────────────────────────────────────────────────────────┐│ │
│  │  │ 🤖 LLM OUTPUT (Python 코드)                                ││ │
│  │  │ ```python                                                  ││ │
│  │  │ today = datetime(2023, 2, 27)                              ││ │
│  │  │ born = today - relativedelta(years=25)                     ││ │
│  │  │ born.strftime('%m/%d/%Y')                                  ││ │
│  │  │ ```                                                        ││ │
│  │  └───────────────────────────────────────────────────────────┘│ │
│  │                           ↓                                    │ │
│  │  ┌───────────────────────────────────────────────────────────┐│ │
│  │  │ ⚙️ EXECUTION (Python Runtime)                              ││ │
│  │  │ exec(llm_output) → "02/27/1998" ✅                         ││ │
│  │  └───────────────────────────────────────────────────────────┘│ │
│  │                                                                │ │
│  └───────────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────┐  ┌───────────────────────────────┐│
│  │ 💪 WHY PAL WORKS           │  │ 🎓 KEY TAKEAWAYS              ││
│  │                             │  │                               ││
│  │ • 계산을 런타임에 위임      │  │ ✓ 자연어 → 코드 → 실행       ││
│  │ • Hallucination 감소       │  │ ✓ 계산 오류 방지              ││
│  │ • 코드 검증 가능            │  │ ✓ 날짜/수학 문제에 효과적    ││
│  │ • LLM 강점 활용             │  │ ✓ Few-shot 예시 필요         ││
│  └─────────────────────────────┘  └───────────────────────────────┘│
└─────────────────────────────────────────────────────────────────────┘
```

**배치 비율**:
- 상단 (15%): 타이틀
- 중상단 (20%): What is PAL + CoT vs PAL (좌우 분할)
- 중앙 (45%): PAL Workflow (가장 넓게!)
- 하단 (20%): Why PAL Works + Key Takeaways (좌우 분할)

# Content Details

## 📖 WHAT IS PAL? (좌측 상단)

**핵심 개념:**
```
자연어 문제 → Python 코드 → 실행 → 정답

"프로그램을 중간 추론 단계로 사용"
```

**출처:** Gao et al. (2022)

## 🆚 CoT vs PAL (우측 상단)

| 비교 | CoT | PAL |
|------|-----|-----|
| 추론 방식 | 자연어 텍스트 | Python 코드 |
| 계산 | LLM이 직접 | 런타임 실행 |
| 정확도 | 계산 오류 가능 | 정확함 |
| 검증 | 어려움 | 코드 검사 가능 |

## ⚡ PAL WORKFLOW (중앙 - 가장 넓게!)

**단계별 예시:**

**Input:**
```
"오늘은 2023년 2월 27일입니다.
정확히 25년 전에 태어났다면
생년월일은 MM/DD/YYYY 형식으로?"
```

**LLM Output (Python):**
```python
from datetime import datetime
from dateutil.relativedelta import relativedelta

today = datetime(2023, 2, 27)
born = today - relativedelta(years=25)
born.strftime('%m/%d/%Y')
```

**Execution:**
```python
exec(llm_output)
# → "02/27/1998" ✅
```

## 💪 WHY PAL WORKS (좌측 하단)

1. **계산 위임**: 수학/논리를 런타임에 맡김
2. **Hallucination 감소**: 계산 결과를 상상 X
3. **검증 가능**: 생성된 코드 디버깅 가능
4. **LLM 강점 활용**: 문제 이해 + 코드 생성

## 🎓 KEY TAKEAWAYS (우측 하단)

- ✓ **자연어 → 코드 → 실행** 파이프라인
- ✓ **계산 오류 방지** (정확한 수학 연산)
- ✓ **날짜/수학 문제**에 특히 효과적
- ✓ **Few-shot 예시** 필요 (코드 패턴 학습)
- ✓ **LangChain 통합** 지원

---

# Instructions

1. **스타일**: 첨부된 이미지의 손필기 느낌, 모눈종이 배경, 아이콘 스타일을 따라주세요.

2. **레이아웃**: 위의 ASCII 다이어그램 구조를 따라 새로 배치해주세요. 첨부 이미지의 레이아웃을 그대로 복사하지 마세요.

3. **언어**: 용어/고유명사는 영어(PAL, Program-Aided Language, CoT, Python, exec() 등), 설명은 한국어.

4. **핵심**: **PAL Workflow (Input → Code → Execution)를 가장 넓고 눈에 띄게** 배치해주세요. 이 콘텐츠의 핵심은 "자연어 → Python 코드 → 실행" 파이프라인입니다.

Please generate the Cheat Sheet now.
